export type User = {
  user: {
    id: string;
    email: string;
    firstName: string;
    businessRole: string;
    gravatarHash: string;
    createdAt: string;
    defaultProjectKey: string;
  };
};
