deployAs:
  # - name: builder
  #   applicationType: merchant-center-custom-application
  #   configuration:
  #     standardConfiguration:
  #       - key: REPO_URL
  #         description: the repo for deploying magic-app
  #         required: false
  #         default: 'git@github.com:commercetools-demo/compose-it.git'
  #       - key: CUSTOM_APPLICATION_ID
  #         description: The Custom Application ID is provided when you add a Custom Application in the Merchant Center
  #         required: true
  #       - key: CLOUD_IDENTIFIER
  #         description: The identifier of the cloud Region that the Custom Application uses to connect to Composable Commerce. The values map to the actual Merchant Center API URL for that Region
  #         default: 'gcp-us'
  #       - key: ENTRY_POINT_URI_PATH
  #         description: The Application entry point URI path identified during registration of Custom Application
  #         required: true
  #         default: 'compose-it-builder'
  #       - key: APPLICATION_URL
  #         description: The Application URL
  #         required: true
  #       - key: INITIAL_PROJECT_KEY
  #         description: development project key
  #         required: true
  - name: magic-app
    applicationType: merchant-center-custom-application
    configuration:
      standardConfiguration:
        - key: APP_KEY
          description: App key
          required: true
        - key: CUSTOM_APPLICATION_ID
          description: The Custom Application ID is provided when you add a Custom Application in the Merchant Center
          required: true
        - key: CLOUD_IDENTIFIER
          description: The identifier of the cloud Region that the Custom Application uses to connect to Composable Commerce. The values map to the actual Merchant Center API URL for that Region
          default: 'gcp-us'
        - key: ENTRY_POINT_URI_PATH
          description: The Application entry point URI path identified during registration of Custom Application
          default: 'compose-it-magic-app'
          required: true
        - key: APPLICATION_URL
          description: The Application URL
          required: true
        - key: INITIAL_PROJECT_KEY
          description: development project key
          required: true
  - name: magic-view
    applicationType: merchant-center-custom-application
    configuration:
      standardConfiguration:
        - key: APP_KEY
          description: App key
          required: true
        - key: CUSTOM_APPLICATION_ID
          description: The Custom Application ID is provided when you add a Custom Application in the Merchant Center
          required: true
        - key: CLOUD_IDENTIFIER
          description: The identifier of the cloud Region that the Custom Application uses to connect to Composable Commerce. The values map to the actual Merchant Center API URL for that Region
          default: 'gcp-us'
        - key: ENTRY_POINT_URI_PATH
          description: The Application entry point URI path identified during registration of Custom Application
          default: 'compose-it-magic-app'
          required: true
        - key: APPLICATION_URL
          description: The Application URL
          required: true
        - key: INITIAL_PROJECT_KEY
          description: development project key
          required: true